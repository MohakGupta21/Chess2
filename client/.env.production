# Used ONLY by `npm run build` (Vite mode "production"), never by `npm run dev`.
# Base URL of the deployed API server, baked into the bundle. Split deploy only —
# leave empty if the same service serves both the API and client/dist.
VITE_API_BASE_URL="https://chess2-m8lg.onrender.com"
